version: "3.4"
services:
  price-entry-realm-deployer:
    build:
      context: ../../
      dockerfile: ./DockerFiles/pre-live-deploy/deploy.Dockerfile
      target: deployer
    container_name: price-entry-realm-deployer
