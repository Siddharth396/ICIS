version: "3.6"
services:
  whitesource:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: runner
    volumes:
      - ../../packages/price-entry-pilet:/source
      - ./whitesource:/whitesource
