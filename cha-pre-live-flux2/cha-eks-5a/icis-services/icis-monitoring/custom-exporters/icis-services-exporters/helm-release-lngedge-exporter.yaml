---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: lngedge-exporter
  namespace: icis-monitoring
spec:
  releaseName: lngedge-exporter
  interval: 5m0s
  chart:
    spec:
      chart: icis-monitoring-exporter
      version: "1.0.1"
      sourceRef:
        kind: HelmRepository
        name: helm-charts
        namespace: flux-system
  upgrade:
    remediation:
      remediateLastFailure: true
      strategy: rollback
  values:
    nameOverride: "lngedge-check-exporter"
    fullnameOverride: "lngedge-check-exporter"

    image:
      repository: artifacts.cha.rbxd.ds/icis-monitoring/lngedge_checks_exporter
      tag: "1.0.0"
      pullPolicy: IfNotPresent

    service:
      type: ClusterIP
      port: 3301

    vault:
      type: kv-v2
      mount: kv/cha/k8s
      path: icis-monitoring/lngedge-vars

    serviceMonitor:
      interval: 30s
      namespace: icis-monitoring
      release: lngedge-exporter

    secrets:
      - name: lngedge-vars
        envName: BASE_URL
        key: IDDN_BASE_URL
      - name: lngedge-vars
        envName: PASSWORD
        key: IDDN_ML_PASSWORD
      - name: lngedge-vars
        envName: USERNAME
        key: IDDN_ML_USERNAME

