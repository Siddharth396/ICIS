filter { 
  if [container] =~ "darwin-price-tool" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "darwin-price-tool_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "darwin-price-tool_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

