filter { 
  if [container] =~ "pforecast-bff" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "pforecast-bff_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "pforecast-bff_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

