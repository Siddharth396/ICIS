filter { 
  if [container] =~ "squidex-event-processor" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "squidex-event-processor_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "squidex-event-processor_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

