filter { 
  if [container] =~ "adtabl" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "adtabl_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "adtabl_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

