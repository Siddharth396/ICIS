filter { 
  if [container] =~ "idsadm" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "idsadm_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "idsadm_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

