filter { 
  if [container] =~ "subscriber-auth-proxy-sboutg" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "subscriber-auth-proxy-sboutg_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "subscriber-auth-proxy-sboutg_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

