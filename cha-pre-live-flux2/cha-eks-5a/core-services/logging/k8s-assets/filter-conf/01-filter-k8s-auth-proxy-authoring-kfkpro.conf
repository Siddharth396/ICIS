filter { 
  if [container] =~ "auth-proxy-authoring-kfkpro" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "auth-proxy-authoring-kfkpro_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "auth-proxy-authoring-kfkpro_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

