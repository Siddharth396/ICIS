apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnector
metadata:
  name: chempf
  namespace: icis-event-streaming
spec:
  config:
    topics: public_integ_petchem_price_forecast_commodity_mapping_1
    database: chempf-integration
    collection: raw_price_forecast_commodity_mapping
    transforms.RenameField.renames: id:_id
