apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnector
metadata:
  name: llmrag
  namespace: icis-event-streaming
spec:
  config:
    topics: public_uat_petchem_price_forecast_commodity_mapping_1
    database: llmrag-systest
    transforms.RenameField.renames: partition_key:_id
    collection: raw.priceForecastCommodityMapping
