---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kibana
  namespace : "genesis-platform"
spec:
  interval: 5m0s
  chart:
    spec:
      chart: kibana
      version: 7.17.3
      sourceRef:
        kind: HelmRepository
        name: elastic
        namespace: genesis-platform

  upgrade:
    remediation:
      remediateLastFailure: true
      strategy: rollback
  
  values:
    replicas: 2
    imageTag: 7.17.3
    elasticsearchHosts: "https://elasticsearch-master:9200"

    extraEnvs:
    - name: 'ELASTICSEARCH_USERNAME'
      valueFrom:
        secretKeyRef:
          name: elastic-credentials
          key: username
    - name: 'ELASTICSEARCH_PASSWORD'
      valueFrom:
        secretKeyRef:
          name: elastic-credentials
          key: password
    - name: 'KIBANA_ENCRYPTION_KEY'
      valueFrom:
        secretKeyRef:
          name: kibana
          key: encryptionkey
    
    kibanaConfig:
      kibana.yml: |
        server.ssl:
          enabled: true
          key: /usr/share/kibana/config/certs/elastic-certificate.pem
          certificate: /usr/share/kibana/config/certs/elastic-certificate.pem
        xpack.security.encryptionKey: ${KIBANA_ENCRYPTION_KEY}
        xpack.encryptedSavedObjects.encryptionKey: ${KIBANA_ENCRYPTION_KEY}
        elasticsearch.ssl:
          certificateAuthorities: /usr/share/kibana/config/certs/elastic-certificate.pem
          verificationMode: certificate
    
    protocol: https
  
    secretMounts:
    - name: elastic-certificate-pem
      secretName: elastic-certificate-pem
      path: /usr/share/kibana/config/certs
      
    ingress:
      enabled: true
      className: icis-nginx-ingress
      annotations:
        cert-manager.io/cluster-issuer: vault-issuer
        external-dns.alpha.kubernetes.io/hostname: kibana.eks-4.genesis.cha.eu-west-1.dev
        lnrs.io/zone-type: private
        nginx.ingress.kubernetes.io/backend-protocol: HTTPS
      path: /
      hosts:
        - kibana.eks-4.genesis.cha.eu-west-1.dev
      tls:
        - hosts:
          - kibana.eks-4.genesis.cha.eu-west-1.dev
          secretName: kibana-genesis.cha-eu-west-1-dev


