---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: elasticsearch
  namespace : "genesis-platform"
spec:
  interval: 5m0s
  chart:
    spec:
      chart: elasticsearch
      version: 7.17.3
      sourceRef:
        kind: HelmRepository
        name: elastic
        namespace: genesis-platform

  upgrade:
    remediation:
      remediateLastFailure: true
      strategy: rollback

  values:
    replicas: 3
    imageTag: 7.17.3
    # Permit co-located instances for solitary minikube virtual machines.
    antiAffinity: "soft"

    # Shrink default JVM heap.
    esJavaOpts: "-Xmx2g -Xms2g"
    clusterHealthCheckParams: "wait_for_status=yellow&timeout=1s"
    resources:
      requests:
        cpu: "1000m"
        memory: "4Gi"
      limits:
        cpu: "2000m"
        memory: "4Gi"

    volumeClaimTemplate:
      storageClassName: ebs-gp3-ext4-retain
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 450Gi
    
    roles:
      master: "true"
      ingest: "true"
      data: "true"

    protocol: https

    esConfig:
      elasticsearch.yml: |
        xpack.security.enabled: true
        xpack.security.transport.ssl.enabled: true
        xpack.security.transport.ssl.verification_mode: certificate
        xpack.security.transport.ssl.keystore.path: /usr/share/elasticsearch/config/certs/elastic-certificates.p12
        xpack.security.transport.ssl.truststore.path: /usr/share/elasticsearch/config/certs/elastic-certificates.p12
        xpack.security.http.ssl.enabled: true
        xpack.security.http.ssl.truststore.path: /usr/share/elasticsearch/config/certs/elastic-certificates.p12
        xpack.security.http.ssl.keystore.path: /usr/share/elasticsearch/config/certs/elastic-certificates.p12
    
    extraEnvs:
      - name: ELASTIC_PASSWORD
        valueFrom:
          secretKeyRef:
            name: elastic-credentials
            key: password
    
    secretMounts:
    - name: elastic-certificates
      secretName: elastic-certificates
      path: /usr/share/elasticsearch/config/certs

    ingress:
      enabled: true
      className: icis-nginx-ingress
      annotations:
        lnrs.io/zone-type: private
        cert-manager.io/cluster-issuer: vault-issuer
        external-dns.alpha.kubernetes.io/hostname: elasticsearch.eks-4.genesis.cha.eu-west-1.dev
        nginx.ingress.kubernetes.io/backend-protocol: HTTPS
      path: /
      hosts:
        - elasticsearch.eks-4.genesis.cha.eu-west-1.dev
      tls:
        - hosts:
          - elasticsearch.eks-4.genesis.cha.eu-west-1.dev
          secretName: elasticsearch-genesis.cha-eu-west-1-dev



