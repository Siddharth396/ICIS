filter { 
  if [container] =~ "pforecast-page-ready-notifier" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "pforecast-page-ready-notifier_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "pforecast-page-ready-notifier_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

