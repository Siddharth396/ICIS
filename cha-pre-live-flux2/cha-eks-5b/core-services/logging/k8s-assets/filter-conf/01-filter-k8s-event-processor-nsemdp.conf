filter { 
  if [container] =~ "event-processor-nsemdp" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "event-processor-nsemdp_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "event-processor-nsemdp_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

