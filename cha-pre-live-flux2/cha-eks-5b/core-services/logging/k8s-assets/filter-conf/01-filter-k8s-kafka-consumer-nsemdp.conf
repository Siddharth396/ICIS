filter { 
  if [container] =~ "kafka-consumer-nsemdp" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "kafka-consumer-nsemdp_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "kafka-consumer-nsemdp_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

