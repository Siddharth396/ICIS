filter { 
  if [container] =~ "cnvsrv" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "cnvsrv_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "cnvsrv_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

