filter { 
  if [container] =~ "prelive-kafka-connectors-monitor" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "prelive-kafka-connectors-monitor_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "prelive-kafka-connectors-monitor_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

