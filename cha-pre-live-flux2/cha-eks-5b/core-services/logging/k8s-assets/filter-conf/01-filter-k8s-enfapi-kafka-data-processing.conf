filter { 
  if [container] =~ "enfapi-kafka-data-processing" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "enfapi-kafka-data-processing_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "enfapi-kafka-data-processing_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

