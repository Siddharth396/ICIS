filter { 
  if [container] =~ "order-kafka-producer" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "order-kafka-producer_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "order-kafka-producer_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

