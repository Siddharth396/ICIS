filter { 
  if [container] =~ "iddn-kafka" {
    if [annotations][seceventlogging/type] {
      mutate {
        id => "iddn-kafka_K8s_Service_Mutate_Filter"
        copy => { "[annotations][seceventlogging/type]" => "service" }
      }
    }
  }

  date {
      id => "iddn-kafka_K8s_Date_Mutate_Filter"
      match  => ["Timestamp", "ISO8601"]
      locale => "en"
      target => "@timestamp"
  }
}

