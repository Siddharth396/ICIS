apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: pgnsvc-mongo-migration-performance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: pgnsvc-mongo-migration
    namespace: flux-system
  filterTags:
    pattern: '^performance-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
