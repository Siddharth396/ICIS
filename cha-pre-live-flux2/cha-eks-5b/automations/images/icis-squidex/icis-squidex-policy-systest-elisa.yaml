apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: icis-squidex-systest-elisa
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: icis-squidex
    namespace: flux-system
  filterTags:
    pattern: '^systest-elisa-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
