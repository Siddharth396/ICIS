apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: icis-squidex-event-processor-systest-chemical-analytics
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: icis-squidex-event-processor
    namespace: flux-system
  filterTags:
    pattern: '^systest-chemicalanalytics-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
