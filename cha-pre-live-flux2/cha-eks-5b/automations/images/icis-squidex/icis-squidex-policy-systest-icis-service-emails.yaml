apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: icis-squidex-systest-icis-service-emails
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: icis-squidex
    namespace: flux-system
  filterTags:
    pattern: '^systest-icis-service-emails-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc



