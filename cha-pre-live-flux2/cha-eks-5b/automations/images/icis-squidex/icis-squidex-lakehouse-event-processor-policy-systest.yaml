apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: icis-squidex-lakehouse-event-processor-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: icis-squidex-lakehouse-event-processor
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
