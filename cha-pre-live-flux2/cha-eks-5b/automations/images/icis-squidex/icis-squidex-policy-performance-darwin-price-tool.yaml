apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: icis-squidex-performance-darwin-price-tool
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: icis-squidex
    namespace: flux-system
  filterTags:
    pattern: '^performance-darwin-price-tool-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc