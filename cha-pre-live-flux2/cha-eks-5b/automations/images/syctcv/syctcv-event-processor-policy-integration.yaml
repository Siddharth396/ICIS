apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: syctcv-event-processor-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: syctcv-event-processor
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
