apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: omspss-authoring-bff-staging
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: omspss-authoring-bff
    namespace: flux-system
  filterTags:
    pattern: '^staging-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
