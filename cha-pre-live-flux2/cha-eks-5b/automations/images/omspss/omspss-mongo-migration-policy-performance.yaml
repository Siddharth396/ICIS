apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: omspss-mongo-migration-performance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: omspss-mongo-migration
    namespace: flux-system
  filterTags:
    pattern: '^performance-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
