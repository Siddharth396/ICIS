apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: order-snowflake-producer-dev
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: order-snowflake-producer
    namespace: flux-system
  filterTags:
    pattern: '^dev-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc