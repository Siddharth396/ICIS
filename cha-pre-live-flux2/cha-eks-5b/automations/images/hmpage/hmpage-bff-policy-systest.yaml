apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name:  hmpage-bff-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: hmpage-app
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc