apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: cvideo-sub-bff-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cvideo-sub-bff
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
