apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: icis-squidex-cli-systest-recyclers-supply-tracker
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: icis-squidex-cli
    namespace: flux-system
  filterTags:
    pattern: '^systest-recyclers-supply-tracker-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
