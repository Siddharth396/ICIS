apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: icis-squidex-cli-uat
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: icis-squidex-cli
    namespace: flux-system
  filterTags:
    pattern: '^uat-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
