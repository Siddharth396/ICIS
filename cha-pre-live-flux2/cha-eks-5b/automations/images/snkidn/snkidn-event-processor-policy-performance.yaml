apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: snkidn-event-processor-performance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: snkidn-event-processor
    namespace: flux-system
  filterTags:
    pattern: '^performance-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
