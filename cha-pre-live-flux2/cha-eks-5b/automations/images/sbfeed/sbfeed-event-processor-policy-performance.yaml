apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: sbfeed-event-processor-performance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sbfeed-event-processor
    namespace: flux-system
  filterTags:
    pattern: '^performance-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
