apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: sbfeed-event-processor-healthcheck-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sbfeed-event-processor-healthcheck
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
