apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: prcent-mongo-migration-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: prcent-mongo-migration
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
