apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: prcent-mongo-seeder-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: prcent-mongo-seeder
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
