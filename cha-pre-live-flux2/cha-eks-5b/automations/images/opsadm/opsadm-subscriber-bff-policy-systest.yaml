apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: opsadm-subscriber-bff-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: opsadm-subscriber-bff
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
