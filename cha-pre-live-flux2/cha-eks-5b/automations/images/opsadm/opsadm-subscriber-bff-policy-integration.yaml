apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: opsadm-subscriber-bff-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: opsadm-subscriber-bff
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
