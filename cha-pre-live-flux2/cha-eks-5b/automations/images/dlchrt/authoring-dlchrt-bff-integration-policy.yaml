apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: authoring-bff-dlchrt-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: authoring-dlchrt-bff
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc