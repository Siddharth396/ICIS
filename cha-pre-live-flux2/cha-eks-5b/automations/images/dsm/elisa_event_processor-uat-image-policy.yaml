apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: event-processor-petana-uat
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: event-processor-petana
    namespace: flux-system
  filterTags:
    pattern: '^uat-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
