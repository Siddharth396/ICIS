apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: infgrm-authoring-bff-staging
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: infgrm-authoring-bff
    namespace: flux-system
  filterTags:
    pattern: '^staging-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
