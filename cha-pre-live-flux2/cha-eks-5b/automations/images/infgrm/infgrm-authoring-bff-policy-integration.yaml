apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: infgrm-authoring-bff-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: infgrm-authoring-bff
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
