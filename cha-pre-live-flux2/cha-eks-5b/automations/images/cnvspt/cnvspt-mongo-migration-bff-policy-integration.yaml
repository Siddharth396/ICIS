apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: cnvspt-mongo-migration-bff-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cnvspt-mongo-migration-bff
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
