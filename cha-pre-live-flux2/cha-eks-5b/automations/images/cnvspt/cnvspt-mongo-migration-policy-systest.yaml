apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: cnvspt-mongo-migration-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cnvspt-mongo-migration
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
