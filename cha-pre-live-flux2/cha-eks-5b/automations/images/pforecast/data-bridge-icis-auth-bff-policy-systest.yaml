apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: pforecast-data-bridge-icis-auth-bff-systest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: pforecast-data-bridge-icis-auth-bff
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
