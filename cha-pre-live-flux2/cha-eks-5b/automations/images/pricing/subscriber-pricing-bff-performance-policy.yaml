apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: subscriber-bff-pricing-performance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: subscriber-bff-pricing
    namespace: flux-system
  filterTags:
    pattern: '^performance-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
