apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: entapi-entitlements-api-staging
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: entapi-entitlements-api
    namespace: flux-system
  filterTags:
    pattern: '^staging-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
