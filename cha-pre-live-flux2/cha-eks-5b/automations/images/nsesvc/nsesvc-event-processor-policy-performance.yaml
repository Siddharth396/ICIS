apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: event-processor-nsesvc-performance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: event-processor-nsesvc
    namespace: flux-system
  filterTags:
    pattern: '^performance-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
