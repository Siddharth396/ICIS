apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: chempf-auth-bff-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: chempf-auth-bff
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
