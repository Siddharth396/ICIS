apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ssplat-env
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ssplat-env
    namespace: flux-system
  filterTags:
    pattern: '^agent-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
