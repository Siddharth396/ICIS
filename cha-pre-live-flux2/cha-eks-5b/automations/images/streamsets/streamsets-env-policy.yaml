apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: streamsets-env
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: streamsets-env
    namespace: flux-system
  filterTags:
    pattern: '^agent-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
