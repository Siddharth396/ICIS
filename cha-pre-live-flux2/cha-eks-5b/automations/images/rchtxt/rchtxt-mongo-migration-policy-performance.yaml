apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: rchtxt-mongo-migration-performance
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: rchtxt-mongo-migration
    namespace: flux-system
  filterTags:
    pattern: '^performance-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc