apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: canvas-mongo-migration-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: canvas-mongo-migration
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
