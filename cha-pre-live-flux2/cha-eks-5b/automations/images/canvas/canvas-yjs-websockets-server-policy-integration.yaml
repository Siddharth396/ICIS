apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: canvas-yjs-websockets-server-integration
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: canvas-yjs-websockets-server
    namespace: flux-system
  filterTags:
    pattern: '^integration-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
