apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: canvas-authoring-bff-systest-three
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: canvas-authoring-three-bff
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
