apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: canvas-mongo-migration-systest-three
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: canvas-mongo-three-migration
    namespace: flux-system
  filterTags:
    pattern: '^systest-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
