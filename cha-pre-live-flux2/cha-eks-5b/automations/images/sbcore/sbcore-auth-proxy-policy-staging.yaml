apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: sbcore-auth-proxy-staging
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sbcore-auth-proxy
    namespace: flux-system
  filterTags:
    pattern: '^staging-(?P<counter>\d+)-.*'
    extract: '$counter'
  policy:
    numerical:
      order: asc
