apiVersion: batch/v1
kind: CronJob
metadata:
  name: backstage-staging-deployment-start
  namespace: icis-backstage-staging
spec:
  schedule: "0 8 * * 1-5"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: scale-up
            image: bitnami/kubectl:latest
            command:
            - /bin/sh
            - -c
            - kubectl scale deployment backstage  -n icis-backstage-staging --replicas=1
          restartPolicy: OnFailure
