apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: job
spec:
  values:
    namespace: "prcent-integration"
    job:
      image:
        repository: artifacts.cha.rbxd.ds/automated/prcent/mongo-migration # {"$imagepolicy": "flux-system:prcent-mongo-migration-integration:name"}
        tag: integration-43-5.4.1-59560 # {"$imagepolicy": "flux-system:prcent-mongo-migration-integration:tag"}
      config_env:
        ENVIRONMENT: integration
        MONGO_DB_NAME: prcent-integration
    vault_secrets:
      name: job-vault-secret-prcent-integration
      VaultK8sRoleName: "kv-prcent-read"
      VaultK8sAuthPath: "cha/eks-b"
      VaultSecretMount: "kv/cha/k8s"
      VaultSecretPath: "prcent/integration/authoring/bff"
