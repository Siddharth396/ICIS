apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: job
spec:
  releaseName: mongo-canvas-integration
  values:
    replicaCount: 1
    namespace: "canvas-integration"
    environment: "integration"
    job:
      image:
        repository: artifacts.cha.rbxd.ds/automated/canvas/mongo-migration # {"$imagepolicy": "flux-system:canvas-mongo-migration-integration:name"}
        tag: integration-155-1.112.0-58490 # {"$imagepolicy": "flux-system:canvas-mongo-migration-integration:tag"}
        pullPolicy: IfNotPresent
      config_env:
        MONGO_DB_CONNECTION_STRING: "mongodb+srv://<username>:<password>@cha-pre-live-ak0n6.mongodb.net/canvas-integration?retryWrites=true&w=majority"
    vault_secrets:
      name: mongo-vault-secret-canvas-integration
      VaultSecretPath: "canvas/integration/authoring/bff"
