apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: job
spec:
  values:
    namespace: "sbfeed-staging"
    job:
      image:
        repository: artifacts.cha.rbxd.ds/automated/sbfeed/sbfeed-mongo-migrator # {"$imagepolicy": "flux-system:sbfeed-mongo-migrator-staging:name"}
        tag: staging-6-1.4.40.20777 # {"$imagepolicy": "flux-system:sbfeed-mongo-migrator-staging:tag"}
      config_env:
        ENVIRONMENT: staging
        MONGO_DB_NAME: sbfeed-staging
    
    vault_secrets:
      name: mongo-migrator-job-vault-secret-sbfeed-staging
      VaultSecretPath: "sbfeed/staging/eventprocessor"         
