apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cronjob
spec:
  values:
    namespace: sbfeed-staging
    cronjob:
      schedule: '"0 * * * *"' # every 1 hour at 0 minute
      image:
        repository: artifacts.cha.rbxd.ds/automated/sbfeed/sbfeed-event-processor-healthcheck # {"$imagepolicy": "flux-system:sbfeed-event-processor-healthcheck-staging:name"}
        tag: staging-12-1.4.41-88 # {"$imagepolicy": "flux-system:sbfeed-event-processor-healthcheck-staging:tag"}
        pullPolicy: IfNotPresent
      config_env:
        ENVIRONMENT: staging
        MONGO_DB_CONNECTION_STRING: "mongodb+srv://<username>:<password>@cha-pre-live-ak0n6.mongodb.net/sbfeed-staging?retryWrites=true&w=majority"
        KAFKA_TOPIC_NAME: public_staging_alerting_notifications_2
    vault_secrets:
      name: event-proc-cronjob-vault-secret-sbfeed-staging
      VaultSecretPath: "sbfeed/staging/eventprocessor"
