apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: job
spec:
  values:
    namespace: "sbfeed-integration"
    job:
      image:
        repository: artifacts.cha.rbxd.ds/automated/sbfeed/sbfeed-mongo-migrator # {"$imagepolicy": "flux-system:sbfeed-mongo-migrator-integration:name"}
        tag: integration-4-1.4.40.4657 # {"$imagepolicy": "flux-system:sbfeed-mongo-migrator-integration:tag"}
      config_env:
        ENVIRONMENT: integration
        MONGO_DB_NAME: sbfeed-integration
    vault_secrets:
      name: mongo-migrator-job-vault-secret-sbfeed-integration
      VaultSecretPath: "sbfeed/integration/eventprocessor"        
