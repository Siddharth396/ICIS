apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cronjob
spec:
  values:
    namespace: sbfeed-integration
    cronjob:
      schedule: '"0 * * * *"' # every 1 hour at 0 minute
      image:
        repository: artifacts.cha.rbxd.ds/automated/sbfeed/sbfeed-event-processor-healthcheck # {"$imagepolicy": "flux-system:sbfeed-event-processor-healthcheck-integration:name"}
        tag: integration-5-1.4.41-tags-1-4-40.1-84 # {"$imagepolicy": "flux-system:sbfeed-event-processor-healthcheck-integration:tag"}
        pullPolicy: IfNotPresent
      config_env:
        ENVIRONMENT: integration
        MONGO_DB_CONNECTION_STRING: "mongodb+srv://<username>:<password>@cha-pre-live-ak0n6.mongodb.net/sbfeed-integration?retryWrites=true&w=majority"
        KAFKA_TOPIC_NAME: public_integration_alerting_notifications_2
    vault_secrets:
      name: event-proc-cronjob-vault-secret-sbfeed-integration
      VaultSecretPath: "sbfeed/integration/eventprocessor"
